Place a small glTF/GLB airliner model here as `aircraft.glb`.

If the file is missing or fails to load, Horizon automatically falls back to a
2D canvas aircraft icon oriented by heading — the page will not crash.
